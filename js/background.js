//script to run on all pages
//use "run_at" : "document_idle" for all pages

//register callback for insertomg text into page
chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    //insert text into active element
});